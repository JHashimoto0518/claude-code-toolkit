# READMEから通知セクションを削除する

## 要点

`README.md` から `## 通知` セクション(64〜74行目付近)をそのまま削除する。他のセクション構成・見出しレベルへの影響はない。専用の設定ファイルやコードは存在しないため、削除対象は `README.md` のみ。

## 実装アプローチ

`README.md` の `## 通知` セクション全体(見出しから次の `## 権限方針の要点` 見出しの直前まで)を削除する。前後のセクション(「使い方」→「権限方針の要点」)がそのままつながる形になる。

## 変更するコンポーネント

| ファイル | 変更内容 |
|---|---|
| `README.md` | `## 通知` セクションを削除 |

## データ構造の変更

なし。

## 影響範囲の分析

### 動作への影響

なし。ドキュメントのみの変更で、コード・設定への影響はない。

### 後方互換性

なし(ドキュメント削除のみ)。

### 永続的ドキュメントへの影響

`README.md` 自体が変更対象。他の `docs/`(このリポジトリには存在しない)・`claude.md` への影響はない。
