# 別デバイスへの通知を実装する

## 実装

- [x] `README.md` の「使い方」と「権限方針の要点」の間に `## 通知` セクションを追加する(design.md「実装アプローチ」のdiffどおり)

## ドキュメント更新

- `docs/` は存在せず、他に影響を受ける永続的ドキュメントもないため対象なし(design.md「永続的ドキュメント(`docs/`)への影響」参照)

## 未実施・検証待ち

- [ ] Remote Controlの実際のペアリング・プッシュ通知動作の確認 — ペアリング(iPhone側でセッションが見える状態)・Proプラン・アプリ側通知権限は確認済みだが、セッション内で複数回テスト通知を送信してもiPhoneへのプッシュは届かなかった。原因はこのリポジトリの記載内容ではなくRemote Control機能側(Anthropic側の配信経路)にあると考えられ、このステアリングのスコープ外として保留する

## todo.md の後処理

- [x] 実装完了後、`todo.md` の `- [ ] 別デバイスへの通知を実装する` を `- [x]` に更新する
