# タスクリスト: todoリストの先頭項目をステアリングとして実行するオプションの追加

## 実装

- [x] `.claude/skills/steering-new/SKILL.md` の `argument-hint` を `[--todo[=<ファイル名>] | 開発タイトル]` に変更する
- [x] 冒頭の引数説明に `--todo` / `--todo=<ファイル名>` の意味を表形式で追記する
- [x] 「手順0: 開発タイトルの決定」を手順1の前に新設する
  - [x] 対象ファイルの決定(省略時 `todo.md`、`--todo=<ファイル名>` 指定時はそのファイル)
  - [x] 先頭の `- [ ] <テキスト>` 行の採用ロジック(見出しは無視、出現順)
  - [x] 元の行全体を手順7で使うために保持する旨の記述
  - [x] 対象ファイル不在・未完了項目なしの場合は作成せず終了する旨の記述
  - [x] `--todo` の後に解釈できないテキストが続く場合はユーザーに確認する旨の記述
- [x] 手順7(実装)の末尾に、`--todo` 経由時の todo 項目チェック更新処理を追記する(完全一致時のみ書き換え、不一致時はユーザーに依頼)

## ドキュメント更新

- [x] 永続的ドキュメント(`docs/` / `README.md` / `README.en.md` / `claude.md`)への影響確認(手順5・design.md 参照)。影響なしと判断済み

## 検証

- [x] 変更後の `SKILL.md` を通読し、手順0〜7の番号・参照(「手順1〜7」「手順7」などの既存記述)が矛盾なく繋がっていることを確認する
- [x] 既存の呼び出し例(`/steering-new [開発タイトル]` を通常のテキストで渡す形)が手順0を素通りする記述になっていることを確認する
- [x] `--todo` / `--todo=<ファイル名>` の説明が `commit` スキルの `--branch` / `--branch=<名前>` と同様の書き方になっていることを確認する

## 未実施・検証待ち(このセッションでは確認できない)

- [ ] `/steering-new --todo` を実際に呼び出し、`todo.md` の先頭未完了項目からステアリングが開始されることの動作確認(スキルの実行はこのセッション内では別途トリガーが必要)
- [ ] 手順7完了後に todo 項目が `- [x]` に更新される一連の流れの実地確認
