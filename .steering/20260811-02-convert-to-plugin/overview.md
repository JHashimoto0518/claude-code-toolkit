# プラグイン化する

## 目的

このリポジトリのリソースをプラグイン化することで、他のプロジェクトでも再利用できるようにする。

## 注意事項

- 配布先のリポジトリをAとする。A独自のスキルやフック等とプラグインが競合することはないか？
- プラグイン化する単位は？今は汎用的なリソースをまとめているが、将来的には分野特化のリソース作成も計画している (例: AWS、Python、go、技術調査など)。その場合、プラグイン化する単位は分野特化のリソースごとにするのか、それとも汎用的なリソースをまとめたものにするのか？
